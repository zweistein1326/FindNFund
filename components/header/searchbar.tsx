import { Input } from "@mui/material"

const Searchbar = () => {
    return (
        <div style={{width:'50vw'}}>
            <Input style={{width:'100%'}} placeholder="Search by name, topic, opportunity"/>
        </div>
    )
}

export default Searchbar